extends layout
block content
  .container
    .row
      .col-sm-4.col-md-offset-4
        h3 #{name}
        p #{bio}
        ul.list-unstyled Skills:
          for skill in skills
            li #{skill}
        a.btn.btn-default(href="/applicants") Back to Applicants